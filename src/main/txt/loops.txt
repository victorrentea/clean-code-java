Loops are a code smell
!search "loops" in https://drive.google.com/file/d/1M3mwL8wIOHvvnfbAsxbn9JB5WPHTpllo

var result = new ArrayList<Order>();
for (var order : list) {
    if (order.getPrice() > 100) {
        result.add(order);
    }
}

result = list.stream().filter(o->o.price > 100).toList()
result = list.sequence.filter{it.price>100}
result = [order for order in list if order.price > 100]
