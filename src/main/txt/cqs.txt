// Command Query Separation = functia ar trebui sa fie o comanda sau o interogare, nu amandoua.
//SOC: desi functia pare ca ne obtine o data, ea de fapt MODIFICA starea parametrului.
// Temporal Coupling: ordinea liniilor conteaza pt motive misterioase

log.debug("Last seen user: " + user.lastLogin()); // QUERY

if (user.lastLogin > 1 year ago) throw new Exception("User Frozen");

// updateAndGetLastLogin(user); // ❌incalca CQS: si schimba si returneaza !
// updateLastLogin(user); // 💖COMMAND numita bine si care declara ca intoarce void
user.lastLogin = computeLastLogin(user); // 💖💖💖si mai bine: escaleaza side-effectul + QUERY


LocalDateTime computeLastLogin(User user) {
    // 23 ifuri
    // alteori return null;
    return LocalDateTime.now(); // side effect
}